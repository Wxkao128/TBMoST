version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.9.20"  #  Python version

python:
  install:
    - requirements: requirements.txt  # set required package

sphinx:
  configuration: docs/conf.py  # Sphinx setup file
